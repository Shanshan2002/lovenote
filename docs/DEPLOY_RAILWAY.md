# 使用 Railway 部署 Lovenote（推荐！）

Railway 完美支持 Node.js 应用，数据会持久化保存！

## 🚀 部署步骤（5分钟完成）

### 1. 访问 Railway
https://railway.app/

### 2. 使用 GitHub 登录
- 点击 "Login with GitHub"
- 授权 Railway

### 3. 创建新项目
- 点击 "New Project"
- 选择 "Deploy from GitHub repo"
- 选择或创建你的 lovenote 仓库

### 4. 自动部署
Railway 会自动：
- ✅ 检测到是 Node.js 项目
- ✅ 运行 `npm install`
- ✅ 启动 `npm start`
- ✅ 分配一个域名

### 5. 获取网址
部署完成后，点击项目，在 Settings > Domains 中可以看到你的网址！

---

## 💾 数据持久化

Railway 提供：
- ✅ 持久化文件系统（你的 JSON 数据会保存）
- ✅ 长期运行的服务器
- ✅ 自动重启
- ✅ 免费额度：每月 $5 credit

---

## 🔧 或者：推送到 GitHub（如果还没有）

```bash
# 在项目目录运行
git add .
git commit -m "Lovenote app ready for deployment"

# 在 GitHub 创建仓库后
git remote add origin https://github.com/你的用户名/lovenote.git
git branch -M main
git push -u origin main
```

然后在 Railway 选择这个仓库部署！

---

## 🌟 Railway vs Vercel

| 功能 | Railway | Vercel |
|------|---------|--------|
| Node.js 服务器 | ✅ 完美支持 | ⚠️ Serverless only |
| 文件持久化 | ✅ 支持 | ❌ 不支持 |
| 数据库 | ✅ 内置支持 | ⚠️ 需要外部 |
| 长期运行 | ✅ 支持 | ❌ 不支持 |
| 免费额度 | $5/月 credit | 免费 |

**结论：Railway 更适合你的应用！**
